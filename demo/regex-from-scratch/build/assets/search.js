window.__TECHBOOK_INDEX__=[{"chapter": "Matching by Hand", "heading": "(intro)", "href": "ch01-matching-by-hand.html", "text": "Seven characters of pattern, twenty-four characters of text, and Python's own regular expression engine needs half a second to answer \"no\". Listing 1-1 asks `(a+)+b` to match a string of a's that contains no `b`, adds one `a` per row, and prints the ratio between each row and the one before it. Watch the last column. \"\"\"How long does a failing match take as the input grows by one character?\"\"\" import re import time def seconds(fn): t0 = time.perf_counter() fn() return time.perf_counter() - t0 if __name__ == \"__main__\": pattern = re.compile(r\"(a+)+b\") previous = None for n in range(18, 25): text = \"a\" * n t = seconds(lambda: pattern.fullmatch(text)) ratio = f\" Every extra character doubles the time. Thirty characters would cost about half a minute and forty would run for most of a day. The machine is an Apple Silicon Mac running CPython 3.14, and every timing in this book comes from it, single run, unless the listing says otherwise. This is not a Python quirk. Russ Cox measured the same doubling in Perl in 2007 and listed [Perl, PCRE, Python, Ruby and Java](https://swtch.com/~rsc/regexp/regexp1.html) as engines built the same way. To see where the doubling comes from, build the matc"}, {"chapter": "Matching by Hand", "heading": "The smallest language worth building", "href": "ch01-matching-by-hand.html#the-smallest-language-worth-building", "text": "The pattern language for this chapter fits in a four-row table. literal `a` that one character dot `.` any one character concatenation `ab` `a` and then `b`, adjacent star `a*` zero or more of the character before it Everything you use daily is missing: `+`, `?`, alternation, parentheses, classes, anchors. That is deliberate, and the precedent is old. When Brian Kernighan needed a matcher small enough to teach from, Rob Pike wrote one for literal, dot, star and the two anchors, and Kernighan later wrote that in his own day-to-day use this class [\"easily accounts for 95 percent of all instances\"](https://www.cs.princeton.edu/courses/archive/spr09/cos333/beautiful.html). That is one experienced person's estimate, not a measurement, and it is enough to justify the cut. Kernighan's account of how long the thirty lines took is hedged in the original: Pike reappeared \"at least as I remember it now\" within \"an hour or two\". The hedge is part of the story. The star is the only construct here that can match in more than one way, so the star is where every interesting question in this chapter comes from. `+`, `?`, alternation and groups arrive in chapter 2, when there is a machine to hold th"}, {"chapter": "Matching by Hand", "heading": "Matching, recursively", "href": "ch01-matching-by-hand.html#matching-recursively", "text": "In Listing 1-2, watch which function consumes pattern and which consumes text. They advance separately, and a star can advance the text by any amount while advancing the pattern by two. \"\"\"A backtracking matcher for literals, dot and star, in the style of Pike's.\"\"\" def match_here(pattern, text): \"\"\"Does `pattern` match at the very start of `text`?\"\"\" if pattern == \"\": return True if len(pattern) > 1 and pattern[1] == \"*\": return match_star(pattern[0], pattern[2:], text) if text and pattern[0] in (\".\", text[0]): return match_here(pattern[ Lines 6 and 7. The base case is an empty pattern , not an empty text. a* has to match \"\" , and a pattern that has run out has nothing left to demand. Because every recursive call strips one construct off the pattern, this is the only place a True can originate. The star test at lines 8 and 9 peeks one character ahead before the current character gets a say. A star is a property of a pa The anchored question composes and the unanchored one does not, which is why `match_here` asks only whether the pattern matches *at the start* of the text. Any suffix can be handed to it for a meaningful answer, so `match_star` does exactly that, and `search` is one"}, {"chapter": "Matching by Hand", "heading": "Where the time goes", "href": "ch01-matching-by-hand.html#where-the-time-goes", "text": "Run `a*a*a*b` against `aaaa` in your head. The first star takes all four a's, the second and third find nothing and keep zero, and `b` meets the empty string and fails. Now the unwinding starts, innermost first. The third star has nothing to give back, nor has the second, so the first gives back one `a`. The second star takes it, the third takes none, `b` fails again. The second gives its `a` back, the third takes it, `b` fails. And so on, until the first star has surrendered all four and every way of splitting four a's across three stars has been tried and rejected. Each star is a loop over how many characters it keeps, and each loop sits inside the one before it. Three stars make three nested loops over an input of length n, which is about n\u00b3 calls. Figure 1-1 draws the two-star case on a three-character input, where the count is small enough to check by hand. The first star of a*a*b takes all three a's, then gives them back one at a time; each give-back restarts the second star, which fails b on every suffix match_star('a', 'a*b', 'aaa') first star keeps 3, then gives back a*b vs '' second star keeps 0 a*b vs 'a' keeps 1, gi Nothing in the tree remembers that `b` already failed "}, {"chapter": "Matching by Hand", "heading": "The incidents", "href": "ch01-matching-by-hand.html#the-incidents", "text": "Neither of the two regex outages below was exponential. On 20 July 2016, Stack Overflow was down for [34 minutes](http://web.archive.org/web/20221011090116/https://www.stackstatus.net/post/147710624694/outage-postmortem-july-20-2016) because a comment held roughly 20,000 consecutive spaces and the home page trimmed trailing whitespace with a pattern that simplifies to `\\s+$`. The engine took the whole run, found a non-space after it, gave the spaces back one at a time, then moved one start position along and did it all again: 20,000 + 19,999 + 19,998 and so on down to 1, which is 199,990,000 steps. The post-mortem's own words are that \"this is not classic catastrophic backtracking\", since the cost is O(n\u00b2) rather than exponential, \"but it was enough\". The fix was a substring function. On 2 July 2019, Cloudflare had a global outage of [about 27 minutes](https://blog.cloudflare.com/details-of-the-cloudflare-outage-on-july-2-2019/) after a firewall rule containing `.*(?:.*=.*)` reached production. The post reduces the culprit to `.*.*=.*` and counts the engine's steps: 23 for `x=x`, 33 for `x=xx`, 45 for `x=xxx`, and 555 for `x=` followed by twenty x's. The post says of those numbers "}, {"chapter": "Matching by Hand", "heading": "Practice", "href": "ch01-matching-by-hand.html#practice", "text": "Exercise 1 Pike's matcher supports ^ . Fill in the blank so that a pattern beginning with ^ is tried at position 0 only. def search(pattern, text): if pattern.startswith(\"^\"): return match_here(________, text) for start in range(len(text) + 1): if match_here(pattern, text[start:]): return True return False Solution pattern[1:] . The anchor is not a character to match but an instruction to search about which start positions are allowed, so match_here never sees it. Kernighan handles it in the same place. Exercise 2 Add + for a single character by rewriting. Complete the stub so that x+ means x followed by x* . def match_here(pattern, text): if pattern == \"\": return True if len(pattern) > 1 and pattern[1] == \"+\": return match_here(________, text) ... Solution pattern[0] + pattern[0] + \"*\" + pattern[2:] . The ordinary literal case matches the one mandatory character on the next call and the star does the rest, at the cost of one extra frame and no new logic. It also shows why + does not change the blow-up class: a+a+b is aa*aa*b , two stars, quadratic. Exercise 3 Wrap match_here in a counting function and rec"}, {"chapter": "Matching by Hand", "heading": "Mental model", "href": "ch01-matching-by-hand.html#mental-model", "text": "A backtracking matcher is a recursive function that asks whether the pattern matches at the start of the text, plus a loop that tries every start. A star keeps the longest run it can and gives back one character per failed attempt. The alternatives live on the call stack and nowhere else. A star is a loop over how many characters it keeps. Adjacent stars nest their loops, so k stars cost about n^k on a failing input. Exponential cost needs a quantifier inside a quantifier. Star height one caps this matcher at polynomial. Recursion depth equals the number of literals in the pattern, so a long pattern fails before a long text does. **Terms introduced:** backtracking \u2014 a search strategy in which each choice point keeps its untried alternatives and a failure returns control to the nearest one that still has some; match_here \u2014 the recursive core of the matcher, which asks only whether the pattern matches at the start of the text; give back \u2014 a star surrendering one character of its run so the rest of the pattern can try again; star height \u2014 the deepest nesting of stars or pluses in a pattern; catastrophic backtracking \u2014 the failing case of a backtracking matcher whose cost grows exponen"}, {"chapter": "Matching by Hand", "heading": "One opinion", "href": "ch01-matching-by-hand.html#one-opinion", "text": "I think every regex tutorial should open with `(a+)+b` and a stopwatch, not with `\\d{3}-\\d{4}`. The syntax tutorials teach that a pattern is a description, and a description sounds free. A stopwatch on Listing 1-1 teaches that a pattern is a program with loops in it, whose running time is decided by input the author never sees. Twenty minutes with a stopwatch does more for a working programmer than a chapter on lookbehind."}, {"chapter": "Matching by Hand", "heading": "Going deeper", "href": "ch01-matching-by-hand.html#going-deeper", "text": "Kernighan, [\"A Regular Expression Matcher\"](https://www.cs.princeton.edu/courses/archive/spr09/cos333/beautiful.html), chapter 1 of *Beautiful Code* (2007). The original thirty lines of C and the argument that recursion is what makes them short; read it for the shortest-match `matchstar`, the other order. Cox, [\"Regular Expression Matching Can Be Simple And Fast\"](https://swtch.com/~rsc/regexp/regexp1.html) (2007). The graph of Perl against Thompson's algorithm on `a?\u207fa\u207f`, and the clearest account of why the doubling happens; chapters 2 and 3 follow its second half. Davis, Coghlan, Servant and Lee, [\"The Impact of Regular Expression Denial of Service in Practice\"](https://davisjam.github.io/files/publications/DavisCoghlanServantLee-EcosystemREDOS-ESECFSE18.pdf) (2018). Measures how common this is: about 1% of unique regexes in npm and PyPI were super-linear, most of them polynomial. Read it before deciding the problem is rare. The matcher forgets every path it abandons. Chapter 2 builds a machine that can hold them all."}, {"chapter": "From Pattern to Machine", "heading": "(intro)", "href": "ch02-pattern-to-machine.html", "text": "Hand `(ab)*` to the matcher from chapter 1 and it goes looking for a literal open parenthesis. `match_here` reads the pattern one character at a time, and the only two-character construct it knows is a star after a single character, so `(ab)*` means `(`, then `a`, then `b`, then any number of `)`. The text `(ab` matches it. The text `abab` does not. Nothing in chapter 1's language can say \"these two characters, repeated\", and its matcher could not be taught to, because the matcher *is* the pattern string, re-read from the front on every call. Ken Thompson's 1968 paper takes the opposite view from its abstract onward. It describes \"an implementation of this method in the form of a compiler\" which [\"accepts a regular expression as source language and produces an IBM 7094 program as object language\"](https://www.oilshell.org/archive/Thompson-1968.pdf); the object program then reads the text. Compile once, match as often as you like, and what runs against the text is a machine. This chapter builds that machine in three files and 127 lines of Python, and ends by counting its states. What you'll learn Parse the eight-construct pattern language into a tree, with precedence decided by the "}, {"chapter": "From Pattern to Machine", "heading": "A tree first", "href": "ch02-pattern-to-machine.html#a-tree-first", "text": "`ab|c` matches `ab` or `c`, not `a` followed by one of `b` or `c`, and nothing in the string says so. You supply the grouping from a precedence rule you may never have seen written down. POSIX writes it down: the ERE table in [\u00a79.4.8](https://pubs.opengroup.org/onlinepubs/9699919799/basedefs/V1_chap09.html) ranks repetition above concatenation and concatenation above alternation, and adds that \"concatenation has a higher order of precedence than alternation\". Russ Cox compresses the same rule to [one sentence](https://swtch.com/~rsc/regexp/regexp1.html): the precedence, \"from weakest to strongest binding, is first alternation, then concatenation, and finally the repetition operators\". So `ab|c` is `(ab)|c`, `ab*` is `a(b*)`, and the parentheses in `(ab)*` exist to override the second of those. The four constructs chapter 1 lacked are plus (`a+`, one or more), optional (`a?`, zero or one), alternation (`a|b`, either), and the group, which makes whatever it encloses a single unit for the operator after it. With literal, dot, concatenation and star, that is the whole language of this book. The structure that records how a pattern groups is an **abstract syntax tree**: one node per con"}, {"chapter": "From Pattern to Machine", "heading": "The machine", "href": "ch02-pattern-to-machine.html#the-machine", "text": "A tree still cannot say \"both\". `Alt(a, b)` names two children and leaves it to whoever walks the tree to choose, which is what chapter 1's matcher did, one child at a time, with a return trip when the first failed. The machine this chapter builds has a **state** for that, one with two outgoing arrows and no character on either, so that being in it means being in both of its successors at once. Thompson's paper has two node types, \"NNODE matches a single character\" and \"CNODE will split the current search path\". Cox's C keeps the same two and adds a terminal. His `struct State` holds a character `c` and two pointers, and the [comment above it](https://swtch.com/~rsc/regexp/nfa.c.txt) is the whole design: \"If c == Match, no arrows out; matching state. If c == Split, unlabeled arrows to out and out1 (if != NULL). If c \u003c 256, labeled arrow with character c to out.\" Figure 2-1 draws the two kinds with arrows. A character state has one arrow and consumes one character to follow it; a split state has two arrows, consumes nothing, and the machine takes both Char('a') out Split out alt consumes a one arrow, one character two arrows, no character: both A character state has one arrow and co"}, {"chapter": "From Pattern to Machine", "heading": "Thompson's construction, one operator at a time", "href": "ch02-pattern-to-machine.html#thompsons-construction-one-operator-at-a-time", "text": "`a|b` and `a?` compile to the same split state. The difference is where the second arrow points. Figure 2-2 draws the wiring for each operator, with `next` standing in for whatever `cont` was when the operator was compiled. Cox's description of the same five shapes is the caption each panel deserves: concatenation \"connects the final arrow of the e1 machine to the start of the e2 machine\"; alternation \"adds a new start state with a choice of either the e1 machine or the e2 machine\"; `e?` \"alternates the e machine with an empty path\"; `e*` \"uses the same alternation but loops a matching e machine back to the start\"; and `e+` \"also creates a loop, but one that requires passing through e at least once\". Five fragments: ab chains two character states; a|b splits to a and b which rejoin; a* enters at a split whose child loops back; a+ enters at the child; a? splits to a or straight to next a b a b a a a Split Spli Each panel is a **fragment**: a sub-machine with one entry state and one exit, the exit being whatever follows. Wiring fragments together, operator by operator, until the last exit is the match state is **Thompson's construction**, and the 1968 paper builds exactly these shape"}, {"chapter": "From Pattern to Machine", "heading": "The size guarantee", "href": "ch02-pattern-to-machine.html#the-size-guarantee", "text": "Nothing in Listing 2-5 allocates more than one `State` per case, and the concatenation case allocates none. Cox states the consequence: the construction \"creates exactly one state per character or metacharacter in the regular expression, excluding parentheses\", so the number of states \"is at most equal to the length of the original regular expression\". Add the match state, which `compile_pattern` makes once, and for this compiler the count is exact rather than a bound. Six patterns are checked in Listing 2-7, with the non-parenthesis character count printed beside the state count. from parse import parse from machine import compile_pattern def count_states(s, seen): if s is None or id(s) in seen: return seen.add(id(s)) count_states(s.out, seen) count_states(s.alt, seen) print(f\"{'pattern': 5} {'states':>6}\") for pattern in (\"a\", \"a*b\", \"a|b\", \"a*a*a*b\", \"(a|b)*c\", \"(ab)+(c|d)?\"): chars = sum(1 for ch in pattern if ch not in \"()\" The count depends on the pattern alone. Whatever the text is, however long, the machine that reads it has m+1 states for a pattern of m non-parenthesis characters, and a machine cannot be in more states than it has. That is the promise chapter 3 collects on"}, {"chapter": "From Pattern to Machine", "heading": "Practice", "href": "ch02-pattern-to-machine.html#practice-2", "text": "Exercise 1 The dataclass reprs in Listing 2-3 are exact and hard to read. This pretty-printer indents one level per child. Fill in the blank so that a Repeat prints as * , + or ? . def show(node, depth=0): pad = \" \" * depth if isinstance(node, Char): print(pad + node.ch) elif isinstance(node, Dot): print(pad + \".\") elif isinstance(node, (Concat, Alt)): print(pad + type(node).__name__) show(node.left, depth + 1) show(node.right, depth + 1) elif isinstance(node, Repeat): print(pad + ________) show(nod Solution \"*\" if node.many and node.min == 0 else \"+\" if node.many else \"?\" . The printer has to reconstruct the operator from the two fields, which is the cost of storing what the compiler needs instead of what the user typed. The parser could keep the operator character as a third field; it does not, because a field the compiler never reads is a place two versions of the truth can disagre def atom(self): ch = self.take() if ch == \"\\\\\": ... if ch == \"(\": ... Solution Take one more character; if it is None , raise SyntaxError(\"trailing backslash\") ; otherwise return Char of it, whatever it is. atom handles the escape entirely, so \\.* parses as a starred literal dot with no change to repe"}, {"chapter": "From Pattern to Machine", "heading": "Mental model", "href": "ch02-pattern-to-machine.html#mental-model-2", "text": "A pattern is parsed once into a tree of five node types. Parentheses shape the tree and then vanish. Precedence is the order of the parser's methods: `alt` calls `concat` calls `repeat` calls `atom`, and each method can only hold operands from the tighter level. A machine state is one of three things: a character with one arrow, a split with two arrows and no character, or the match state with none. Each node is compiled knowing the state after it, so concatenation makes no state and no arrow is ever patched. The machine is built from the match state backwards. Star and plus are the same loop entered at opposite ends; optional is a split whose second arrow skips the child. A pattern of m non-parenthesis characters compiles to exactly m+1 states, whatever the text. **Terms introduced:** abstract syntax tree \u2014 the tree a pattern parses into, one node per construct with its operands as children and no record of the parentheses; recursive descent \u2014 a parser made of one function per grammar rule, each calling the rules it depends on; precedence level \u2014 one row of the operator-binding order, which in a recursive-descent parser is one method; state \u2014 a node of the machine, holding at most"}, {"chapter": "From Pattern to Machine", "heading": "One opinion", "href": "ch02-pattern-to-machine.html#one-opinion-2", "text": "I think the continuation-style compiler is the version to teach, and Cox's patch-list version is the version to read. Teach from Listing 2-5 because it is one function whose only subtle line is the star's allocation order, and that line is subtle for a reason the reader can see. Read Cox's C because it is the 1968 paper's method, a stack of fragments with holes patched afterwards, and it is how you find out that Thompson compiled straight to machine code with the same structure. Learn the construction from one and the history from the other."}, {"chapter": "From Pattern to Machine", "heading": "Going deeper", "href": "ch02-pattern-to-machine.html#going-deeper-2", "text": "Thompson, [\"Programming Techniques: Regular expression search algorithm\"](https://www.oilshell.org/archive/Thompson-1968.pdf), *Communications of the ACM*, 1968. Four pages: the machine-code lists, the `a**` remark, and the note chapter 3 depends on. Cox, [\"Regular Expression Matching Can Be Simple And Fast\"](https://swtch.com/~rsc/regexp/regexp1.html) (2007), and its [nfa.c](https://swtch.com/~rsc/regexp/nfa.c.txt). About 200 lines of C, postfix in, patch lists throughout. Might, [\"Parsing regular expressions with recursive descent\"](https://matt.might.net/articles/parsing-regex-with-recursive-descent/). The four-level grammar most tutorials copy. Aho, Lam, Sethi and Ullman, *Compilers*, 2nd ed., \u00a73.7, for the textbook bound of at most twice the operators and operands. I read the argument in a [lecture transcript](https://cse.iitkgp.ac.in/~bivasm/notes/scribe/11CS10055.pdf), not the book. The machine for `(a|b)*c` can stand in three states at once. Chapter 3 runs it, and finds out what \"at once\" costs."}];
