window.__TECHBOOK_INDEX__=[{"chapter": "Shapes of a Chapter", "heading": "(intro)", "href": "ch01-shapes.html", "text": " Every chapter opens with a concrete problem, not a promise about what the chapter will cover. This one opens with a broken program. What you'll learn Read a fenced block's contract and know whether its output was verified Tell a sidenote from a callout, and know when each earns its place Predict what a snippet prints before running it Assumes you know Basic Python syntax. Nothing else. ~20 min \u00b7 4 exercises"}, {"chapter": "Shapes of a Chapter", "heading": "The problem", "href": "ch01-shapes.html#the-problem", "text": "Here is a function that looks right and is wrong. The bug is subtle enough that it survived code review twice in the original codebase this is drawn from. def average(values): total = sum(values) return total / len(values) print(average([1, 2, 3, 4])) That works. Now watch what happens on the empty list. def average(values): return sum(values) / len(values) average([]) This trips people up An empty sequence is not an error in sum \u2014 it returns 0 quite happily. The failure surfaces one operation later, in the division, which is why the traceback points somewhere the reader did not expect. Predict: what does average([2]) return \u2014 2 or 2.0 ? 2.0 . True division always produces a float in Python 3, even when the operands divide evenly. This matters the moment you compare the result to an integer with is rather than == ."}, {"chapter": "Shapes of a Chapter", "heading": "A mental model", "href": "ch01-shapes.html#a-mental-model", "text": "Values flow into sum, then into division by the count values sum() / len() Figure 1-1. The count is read separately from the sum, so an empty input only fails at the second step. `[1,2,3,4]` 10 4 `2.5` `[2]` 2 1 `2.0` `[]` 0 0 raises"}, {"chapter": "Shapes of a Chapter", "heading": "Practice", "href": "ch01-shapes.html#practice", "text": "Exercise Fill the single blank so the function returns None for empty input. def average(values): if ____: return None return sum(values) / len(values) Solution not values . Prefer it over len(values) == 0 : it reads as the question being asked, and it works for any sequence. Exercise Now write it from scratch, raising ValueError instead."}, {"chapter": "Shapes of a Chapter", "heading": "What to remember", "href": "ch01-shapes.html#what-to-remember", "text": "`sum` of an empty sequence is `0`, not an error Errors often surface one step after the operation that caused them Nested lists work too: like this and this"}, {"chapter": "Shapes of a Chapter", "heading": "Inline regressions", "href": "ch01-shapes.html#inline-regressions", "text": "A link whose text holds code, [the `re` reference](https://docs.python.org/3/library/re.html), and a table with backslashes (regression: nested placeholders and `\\d`). class `[a-z]`, `\\d` backreference `\\1` pipe `a|b`"}];
